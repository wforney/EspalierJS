/**
 * Define a button that is showin in the far right-hand
 * column of a row for a given record.
 */
export declare class TableButton {
    buttonClass: string;
    iconClass: string;
    title: string;
    onClick: Function;
    constructor(buttonClass: string, iconClass: string, title: string, onClick: Function);
}
